Django~=5.2
PyYAML==6.0.2
setuptools==78.1.1
opentelemetry-api>=1.27,<2
# SDK is a *test* dependency only. The library itself must never require it —
# see the docstring of `record_exception_on_span`.
opentelemetry-sdk>=1.27,<2
wheel==0.46.2
twine==6.1.0
tox==4.24.1
pytest==9.0.3
