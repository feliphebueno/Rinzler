[tool.black]
    line-length = 120

[tool.flake8]
    max-line-length = 120
    exclude = ".tox,.git,*/migrations/*,*/static/CACHE/*,docs,venv"

[build-system]
requires = ["Django~=5.2", "PyYAML==6.0.2", "setuptools==78.1.1"]
